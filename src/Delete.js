import React from 'react'

const Delete = (props) => <input style={{backgroundColor: "#ffb347"}} type='button' value='Delete' onClick={()=>props.delete(props.d)}/>
                                 
    
  


export default Delete
